cara menjalankan
1. jalankan file requirements.txt dengan cara -> pip install -r requirements.txt
2. jalankan file model_training.ipynb sampai selesai menghasilkan file pkl
3. jalankan file app.py dengan cara -> streamlit run app.py